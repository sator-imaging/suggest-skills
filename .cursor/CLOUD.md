## Pull requests (mandatory)

- NEVER use ManagePullRequest or any built-in platform PR tool.
- After push, create/update PRs ONLY with `gh pr create` / `gh pr edit`.
- `GH_TOKEN` is set; use `gh` with it.
- If `gh pr create` fails, stop and report — do not fall back to ManagePullRequest.
